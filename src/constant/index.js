import { easeInOutList } from './easeInOutList';
import { easeInList } from './easeInList';
import { easeOutList } from './easeOutList';

const easingList = {
  easeInOut: easeInOutList,
  easeIn: easeInList,
  easeOut: easeOutList,
};

export default { easingList };
